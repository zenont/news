query TopHeadlinesQuery($country: String) {
  headlines(country: $country) {
    articles {
			id,
      author,
      title,
      url,
      description,
      urlToImage,
      publishedAt
    }
    total
  }
}
